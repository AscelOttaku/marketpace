spring:
  application:
    name: market-service

  datasource:
    url: jdbc:postgresql://localhost:5432/market
    username: postgres
    password: Kanybek20065732
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8090

max:
  body:
    length: 200

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    lease-expiration-duration-in-seconds: 90
    lease-renewal-interval-in-seconds: 40
    prefer-ip-address: true